import { defineConfig } from 'dumi';

export default defineConfig({
  title: '小贺前端',
  favicon: 'hly.jpg',
  logo: 'hly.jpg',
  outputPath: 'docs-dist',
  mode: 'site',
  base: './',
  publicPath: './',
});
